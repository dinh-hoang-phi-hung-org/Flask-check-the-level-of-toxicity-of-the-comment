flask==2.2.3
werkzeug==2.2.3
flask-cors==3.0.10
# torch is installed separately in Dockerfile with CPU-only index to avoid bundling CUDA (~2.5GB)
# /venv/bin/pip install torch --index-url https://download.pytorch.org/whl/cpu
transformers>=4.18.0
pypandoc>=1.8.0
sentencepiece>=0.1.96
python-dotenv==1.1.0
gunicorn